name: TU_Tech
services:
  react:
    env_file:
      - .env.production
    build: .
    deploy:
      mode: replicated
      replicas: 2
    links:
      - load_balancer:load_balancer
  load_balancer:
    image: nginx
    container_name: fe-lb
    ports:
      - 81:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
